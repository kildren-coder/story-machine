# Story Machine — 项目规格（SPEC）

> 本文是本项目**唯一可信的需求来源**。面向接手的 Agent（如 Claude Code）：读完本文即可开工，不需要再和用户重新对齐一遍。
> 与用户临时口头描述冲突时，以本文记录的设计原则为准，除非用户明确要求变更。
>
> **本文替代 `audio-obsidian-pipeline-spec.md`（2026-07-27 重写）**。前身是桌面端聊天窗口里攒出来的，把「自动生成知识图谱」当成了目标；本次重写把目标收窄回「整理直播素材、辅助人吸收」，砍掉了自动综合那一整层。前身的**阶段 0/1 全部实测结论原样保留**（它们与本次改动无关），原档见 git 历史。

---

## 1. 目的与边界

### 1.1 真实目的（两条，其余全部是派生）

> **(A) 快速了解并吸收一集内容**
> **(B) 学会主播的分析框架，尤其是他们的信息源**

用户日常要处理大量时长约 3 小时的政经类音频（时事评论 / 跨国跨百年的宏观历史脉络）。听不过来，也不满足于听完就忘。软件要做的是把这些内容变成**可查、可比对、能反复回到原话**的结构，让人在此之上做吸收和综合。

**判断「要不要加这个功能」的唯一标准**：它服务 (A) 还是 (B)？两个都不是，就不做——无论它听起来多酷。

### 1.2 三层难度分级，以及被删掉的那一层

| 层 | 目标 | 判定 |
|---|---|---|
| ① 归档与检索 | 音频→转写→结构化→可搜索 | **软件做，完全可行** |
| ② 脉络与素材 | 断言、因果、时间轴、素材沉淀 | **软件做，需要设计** |
| ③ 判真假 / 分析意图 / 预测未来 | —— | **删除。不是「以后再做」** |

③ 被删除的理由：这不是一个软件能做的事。前身 spec 把它当目标，于是地基怎么打都不够。删掉之后项目立刻可行。

③ 唯一能实现的形式是**簿记而非推理**：长期记录「这个人当时预测了什么」+「后来兑现了吗」。不靠模型判断，靠记账。实现方式是 `prediction` 类断言 + 兑现回填字段（§5.3），以及人工触发的周检（§4.6）。

### 1.3 软件与人的职责边界

| | 职责 |
|---|---|
| **软件** | 转写、切分、抽取、归档、检索、比对、把碎片摆到人面前（第 ①②层） |
| **人** | 综合、判断、写脉络（第 ③层意义上的「理解」） |

> **不要让 AI 生成脉络。**
> 理由一：它做不好——让 LLM 综合几百条碎片断言写历史脉络，产出会流畅但空洞，是正确的废话，且会悄悄编造因果。
> 理由二（更关键）：**这一层正是用户真正想要的东西本身**。判断力产生于「综合」这个动作。把综合外包给 AI，等于把想要的成果本身外包掉。

### 1.4 明确移出范围

以下曾在 `docs/vision.md` 立过「四愿景」，按 §1.1 的标准重新裁定：

| 原愿景 | 处置 |
|---|---|
| 求真引擎 | **降级为按需工具**：不建查证调度器、不做到期提醒。就是「对某几行接 Claude Code 去溯源」，人触发（§4.6） |
| 资产分析框架 | **移出**。接 FRED 拉数据序列服务的是交易决策，不是「吸收直播」 |
| 故事素材库 | **移出**，但保留 `素材::` 零成本标记（抽取时顺手打一笔，§5.3） |
| 信源信用档案 | **拆开**：算「历史准确率」的部分移出；**「渠道索引」升为一等公民**（`type:: channel`），因为它就是目的 (B) |

`docs/vision.md` 须按本表改写。四张 `vision` 标签的 issue（#43–#46）保留为灵感堆放地，但不再是路线图。

### 1.5 防膨胀检查表

每次想加功能时问：

- 这个功能对应**哪一个我今天就要用的输出**？
- 它是不是在偷偷把 ③（判真假/预测）加回来？
- 它能不能**推迟到真正感到痛**的时候再做？

---

## 2. 硬件与账号现状

- **笔记本电脑**：主力设备。日常开发、Obsidian 库、Claude Code 都在这台机器上。
- **房间内 PC**：RTX 5070（12GB 显存），平时可能睡眠/关机，专门跑本地语音识别。
- **Claude 订阅**：Pro，可能升 Max 5x（视额度消耗决定）。Claude 与 Claude Code 共享额度池，5 小时滚动窗口 + 每周上限双层限额。同一账号还跑另一个 AFK 项目，共享额度。
- ~~**Gemini Flash**~~ **已废弃（#14 实测）**：Flash 在关系/论断抽取上不达标，全流程 LLM 改走 Claude Code，见 §7。连带作废 #6 的免费额度测算、#35 §2.3 的国产权重审查分析、本地推理的显存/上下文上限问题。
- **NotebookLM**：免费版，仅作个人理解的并行工具，**不接入自动化主链**。

---

## 3. 总体架构

```
[笔记本：Obsidian + Claude Code]
         │   (Tailscale + SSH 桥接)
         ▼
[房间PC：RTX 5070 + faster-whisper]
```

- **Tailscale** 建立两机固定虚拟内网，避免端口转发与 IP 漂移，笔记本在外也能连上 PC。
- PC 开 **SSH 服务**（Windows OpenSSH Server），密钥免密登录。
- 建议给 PC 配 **Wake-on-LAN**，笔记本可远程唤醒后派发任务。
- 笔记本本地需要包装脚本：上传音频（`scp`）→ 远程触发转写（`ssh`）→ 取回逐字稿（`scp`）。应封装成 CLI 子命令，可被 Claude Code 直接调用。
- ASR 之后的所有环节都在笔记本本地完成，不再依赖 PC。

---

## 4. 处理流程

```
0  音频 ──► 逐字稿 JSON（正本，永久）──► 修复 pass
1  分块（20–30 min，重叠 1–2 min）
2  抽取 ──► 断言表初稿（_review/）        ← 三闸门 + ⛔ 可疑标注
2.5 实体匹配预检（零 API 精确匹配 + 建议合并，嵌进草稿）
3  人工审核 ★ 用户唯一花时间的地方
4  ├─ 断言行落进 EP 笔记（＝汇入总库，总库是一条查询）
   └─ 断言表 + 逐字稿 ──► 精简材料（按主题分段）
5  人：读材料 + 查总库 ──► 写脉络笔记
   按需：对特定行溯源查证 / 每周过一遍待检验预测
```

### 阶段 0：音频转写（PC 执行，经桥接触发）

**本阶段与本次架构改动无关，全部结论沿用，一行不改。**

- 工具：`faster-whisper`，模型 `large-v3`。配置已在 #11 照真机实测固化：`float16` / `batch_size=16` / VAD 开 / `word_timestamps=True` / `language="zh"`（实测 43 倍实时）。#24 收尾实测补证：`zh` 强制与 auto 检测全篇输出逐字节一致，英文短语在 `zh` 下完整存活、切换处词时间戳可靠，`multilingual` 无增益不启用；VAD 关闭在音乐段大规模幻觉（且批处理模式架构上必须 VAD）。**`language="zh"` + VAD 开是终局配置，不再复议。**
- RTX 5070 是 Blackwell 架构，需要 CUDA 12.8+ 及对应 PyTorch/CTranslate2 构建；环境报错优先排查这一点。
- **说话人分离子步**（#11/#15）：转写后同机顺序跑 3D-Speaker（CN-EN CAM++），纯 CPU、独立 venv、不开重叠检测；K 默认自动估计上限 4，CLI 可 `--speakers N` 锁死；用词时间戳在切换点拆段后挂 `speaker` 标签。**禁用 `pyannote.audio` 4.x**（显存尖峰 bug）。
- **输出**（#11）：**JSON 正本**——段级 `{start, end, speaker, text}`，浮点秒，匿名 `SPEAKER_XX`，**永久不可变**；`EP{n}.txt` 为派生渲染 `[HH:MM:SS] SPEAKER_00: ……`。中英混杂保留原文语言，不强制音译。匿名→身份映射每集一次人工点名，落 `EP{n}.meta.json`，转写后、提取前填（声纹自动化 → #27）。
- **热词库机制**（#11/#24）：
  1. `initial_prompt` **封死**（#24 实测 prompt 泄漏幻觉，撞红线），改用 faster-whisper 的 `hotwords` 参数。
  2. 持久 `hotwords.json` 只收**多字词**（专名/成语）；短字母缩写不收录，归修复 pass 的语境规则。
  3. 提取前跑一道 **LLM 修复 pass**（Claude Code 无头，`--model sonnet --effort low`，只修同音词、禁增删）：按阶段 1 的块无重叠顺序处理；出口设机械闸门（段数/时间戳/speaker 不变，逐段编辑距离超 ~15% 回退原文）；每块附产新专名清单 → 集内滚动词表 `EP{n}.glossary.json`；差分 `EP{n}.repairs.json` 供抽查。**修复稿是派生文件，原始正本永久保留。**
  4. 词表入口只有人工回流：阶段 3 审核时把确认的新词补进 `hotwords.json`（滚动词表不自动入库）。
  5. 热词库是长期资产，随集数递增完善，**不要每次重新生成**；与别名库共用一份数据。
  6. **硬边界**：`hotwords` 有 **223 token 静默截断上限**（faster-whisper 源码实证 `max_length // 2 - 1`，超出无告警丢弃；与 `initial_prompt` 共享 prompt 窗口，#18）。注入前计 token、超限告警。库攒大后「按集选词」是运营问题，实跑逼近上限再定。

### 阶段 1：分块

- 逐字稿按 **20–30 分钟**切块，块间 **1–2 分钟重叠**，尽量在句子边界切。
- 3 小时逐字稿约 4–6 万字（60–90k tokens），不应一次性处理；分块后单块 1–2 万字，保持模型对细节的注意力，控制在 **120k 上下文 smart zone** 内。

### 阶段 2：抽取 → 断言表初稿

对每个分块**无头调用** Claude Code（`claude -p --model opus --effort high --allowedTools ""`），产出**断言行**（数据模型见 §5）。

**这是全流水线的质量杠杆点。** 下游再没有任何 AI 环节能兜底——人是唯一的下游。因此：

- **`--allowedTools ""` 是硬要求**：提取不需要任何工具，禁用可消掉「agent 跑去读文件/搜索」这一整类失败模式，也让每次调用可复现。
- **不要用 `--bare`**：该模式强制走 `ANTHROPIC_API_KEY`，计入 API credits 而非订阅额度。要清洁上下文用 `--system-prompt-file` + `--add-dir`。
- **不要因为「新架构只是攒素材」就降级引擎。** 新架构缩小的是**范围**（不综合、不织网），不是**单条抽取的质量要求**。#14 实测：Flash 能答「出现过谁」，答不了「论证了什么」——而 `causal` 拆 cause/effect 恰恰就是「论证了什么」。Sonnet 在此环节**未测**，它是第一个该测的降级点（同一分块 + 三闸门对照）。

**已知实体名单注入**（#30）：把 `_index/entities.json` 的标准名 + 别名注入 prompt，抽取时直接复用规范名（最便宜的一道去重）；像但不在名单的表述标「建议合并 + 理由」，绝不自动合并。

**出口校验（schema + 三闸门，失败即重试，#14 实测拍板）**——无头 LLM 没有 GBNF 那样的硬约束，出口必须补机械校验。写入 `_review/` 前逐条过：

| # | 闸门 | 判据 |
|---|---|---|
| 0 | schema 校验 | 字段齐全、`type`/`情态`/`事件时间精度` 在枚举内。不合法直接重跑该块（最多 N 次，仍失败落 `_failed/` 并报警，**不静默丢块**） |
| 1 | 引文逐字存在 | `source_quote` 去时间戳、去空白后在本块原文子串命中 |
| 2 | 实体名在自己引文里 | 防「引文真实但与该实体无关」（实测抓出 6 条挂同一句无关引文的派生项） |
| 3 | 时间戳真实 | `录音时间戳` 必须是本块真实出现过的。编造的时间戳会把审核者带到错误位置，直接废掉审核面的工作机制 |

闸门**只判机械纪律，不判语义**。三条全过仍可能是废条目。未过闸门的条目**不丢弃**，标记后照常进草稿供人处置。三闸门与引擎无关，换引擎不需改。

**⛔ 对抗式初筛（#47）**：抽取的同时，让模型对自己的产出做一遍原地标注——把可疑条目打 ⛔ 并写明理由，**不排序、不过滤**。典型可疑类型：语气固化（原文「应该是」被写成确定断言）、日期与常识冲突、悬空指代未消解。这一层是给阶段 3 减负的，不是质量闸门。

### 阶段 2.5：实体匹配预检（瘦身版）

结果**嵌进 `_review/` 草稿本身**——人在阶段 3 单触点顺手裁决，不另设第二条审核动线。

- **程序级匹配（零 API）**：草稿实体对 `entities.json` 做精确名/别名/机械归一化（大小写、首尾空白、全半角、简繁）匹配，命中即自动采用规范名。**零启发式规则**：子串包含类模式（凯恩斯学派 ⊃ 凯恩斯）恰是本题材歧义最密集处，永不自动合并。
- **中英对照表**：人工编写的导入式种子文件（LLM 不得添条目），启动时同步进 `entities.json` 成别名对象；冲突在导入时报错拒导。**运行时真相只有 `entities.json` 一处。**
- **模糊命中不自动合并**：Claude Code 无头（`--model sonnet --effort medium`）生成「建议合并：X→Y，理由…」嵌进草稿，供阶段 3 点头/摇头。
- **无 embedding**：候选发现靠 §4 阶段 2 的名单注入 + 抽取引擎兼职。#33（embedding 消歧层）**关闭**——新架构不再自动生成实体笔记，消歧的代价大幅下降，不值得引入这一层。实跑证明建议漏得多再议。
- **裁决回流即白名单**：点头 → 落地为该实体的别名对象，下次零 API 精确命中；摇头 → 该表述自立新实体，同样短路不再重问。**不建独立的「决议缓存」**（会固化错判绕过闸门）。

### 阶段 3：人工审核（不可省略）

用户在 Obsidian 中直接打开 `_review/` 草稿校对。动作只有四种：

1. 改 `type` / `情态`（⛔ 标出来的多半是这个）
2. 删明显无意义或幻觉性的行
3. 存疑打 `?`，供后续溯源重点处理
4. 对「建议合并」点头/摇头；一眼判不了的按新实体建档 + 「疑似重复」标记，留待后续集数积累上下文再裁

审完把文件挪出 `_review/` 即为「审核通过」。

> **审核负担红线**：人工审核只落在**提取产物**上。任何要求用户通读逐字稿的设计都是跑偏。

**校对配对数据（廉价保险，不可省略）**：用户的校对动作本身就是高质量标注，**只能在校对当下被捕获，事后无法补做**。

- 阶段 2 写出草稿时同时写只读快照 `_pairs/EP{n}/chunk{k}_pre.md`
- 阶段 3 确认信号触发时把校对后版本存为 `_pairs/EP{n}/chunk{k}_post.md`
- `_pre` 必须记录产出引擎（frontmatter `engine:`）
- 两份均为永久资产。MVP **不对这批数据做任何加工**（不 diff、不统计、不训练），只负责无损保存

| 用途 | 是否可用 |
|---|---|
| 提示词迭代的回归测试集 | ✅ 始终可用 |
| 提取质量随时间的变化度量 | ✅ 始终可用 |
| 训练/微调任何模型 | ⚠️ 仅限 `engine:` 为非 Anthropic 模型的配对 |

Anthropic 使用条款限制用其输出开发或训练 AI 模型，不因「仅自用」豁免。全流程走 Claude Code 后，**训练用途默认关闭**，按最严口径执行。`engine:` 字段让这条边界机械可判定。

### 阶段 4：落库 + 精简材料

审核通过后并行两件事。

#### 4a. 断言行落进 EP 笔记（＝汇入总库）

断言行写进 `10-Episodes/EP{n}.md`，形态是**带行内字段的 list item**（§5.1）。

> **总库不是一个文件，是一条查询。** 断言行物理上躺在各自的 EP 笔记里；「小布什时间轴」「待检验预测队列」都是保存下来的 Dataview 查询。不搬运、不同步、不会有两份真相。

#### 4b. 精简材料生成

**输入**：审核过的断言表 + 逐字稿。**输出**：`15-Materials/EP{n}.md`，按主题分段、去掉废话、时间戳粗粒度，**压到原文约 1/10（4–6 千字，一口气能读完）**。

**为什么必须先有断言表**：「按主题聚拢」内在需要全集视野（同一主题会散在第 20/80/150 分钟），但 3 小时逐字稿有 60–90k token，硬塞进去注意力涣散。审核过的断言表已经是全集浓缩（几千字），**它就是全局视野的载体**。所以：

```
第 1 遍  断言表（已审核）→ 主题骨架：本集分 N 个主题，各覆盖哪些时间戳区间
         输入很小，全局视野来自表而非逐字稿
第 2 遍  逐主题，只喂它涉及的那几段逐字稿 → 写该主题的段落
         输入受控，且天然带靶子（该主题下的断言必须有对应内容）
```

**允许与禁止（红线，写进 prompt）**：

| 允许 | 禁止 |
|---|---|
| **删**：口播广告、寒暄、催三连、完全跑题的闲聊、逐字重复 | **增**：不许补任何逐字稿里没有的背景知识 |
| **重排**：把散在三处的同一主题聚到一起 | **综合**：不许把几个观点归纳成一个结论 |
| **合并时间戳**：细碎的 → 主题段落的区间 | **改口径**：不许把「应该是」写成「是」 |

> **不得删除的一类「废话」**：限定词（「应该是」「我不确定」「大概率」）、自我修正（「刚才说错了」）、消息来源交代（「我看到某某报道说」）、犹豫与转折。
> 它们语法上冗余、读起来磕巴，但**恰恰是判断主播成色的全部证据**。一份把「我不确定」和「肯定是」抹平成同一种语气的材料，会让人系统性高估他。这与阶段 2 挡「语气固化」是同一条红线，两个环节都要写。

**其余约束**：

- **主题段内保留时序，段间按主播首次触及的顺序排。** 全局打乱会丢掉「他是怎么想到这的」——思路走向本身就是分析框架的一部分。
- **每段带时间戳区间**：`## 战时授权的空心化 [00:23:30–00:31:10, 01:42:00–01:48:30]`，随时跳回原文。
- **断言表独立于材料产出**（从逐字稿直接抽，不从材料里抽），两者互为校验：某条断言在材料里找不到对应段落 = 材料漏了。

### 阶段 5：人做深加工

- 读 `15-Materials/EP{n}.md` 吸收本集（目的 A）
- 查总库做跨集比对（目的 B）：同一结果的不同因果解释、某人的时间轴、某主题的所有断言
- 顺手回填 `主题::`（§5.4 说明为什么落点在这里）
- 写脉络笔记 `30-Threads/*.md`——**永不自动生成**

### 4.6 按需工具（人触发，非流程）

| 工具 | 触发 | 做什么 |
|---|---|---|
| **溯源查证** | 你指着某几行说「查这个」 | Claude Code 联网找原始信源（政府文件、当事媒体、学术资料），**附判定 + 依据链接**。只对 `fact` / `causal` 有意义，`judgment` 不查 |
| **预测周检** | 每周你打开待检验队列 | 把那一屏丢给 Claude Code 检索「这些兑现了吗」，你看结论、回填 `检验状态` |

两者都**不做自动化**：不建调度器、不做到期提醒、不自动回填。理由见 §1.2——这是簿记，仪式本身不值得写代码。周检要成立只需要队列可查，schema 已经支持（§6.2）。

---

## 5. 数据模型

### 5.1 断言＝带行内字段的 list item

```markdown
- **A-02** 911 后小布什长期担心被斩首，因此设立八人帮作为指挥「备份」
    type:: causal
    情态:: 确定
    谁说的:: 主播
    归因类型:: 无外部归因
    录音时间戳:: 00:29:00
    事件时间原文:: 911之后
    事件时间:: 2001-09-11
    事件时间精度:: day
    cause:: 911 之后小布什在相当一段时间里非常害怕自己被斩首
    effect:: 需要一个非正式小圈子，在正式指挥链断掉时仍能就政治经济事务做决策
    实体:: [[小布什]], [[美国国会八人帮]]
    主题::
    source_quote:: 之所以当时小布什要设置这样的一个非正式的组织
```

`channel` 行（开发优先级第 3 位，第一个真正要抽的东西）：

```markdown
- **A-11** 美债持有人结构可在财政部 TIC 月报查到
    type:: channel
    情态:: 确定
    谁说的:: 主播
    录音时间戳:: 01:12:40
    渠道类型:: 数据源
    名称:: 美国财政部 TIC 月报
    取数地址:: 月度持有人明细表，按国别拆
    链接::
    原话口径:: 谁在买
    为什么值得看:: 比新闻里转述的口径干净
    实体:: [[美国国债]], [[美国财政部]]
    主题:: 美债, 资金流向
    source_quote:: 你们去财政部那个 TIC 的月报里面能查到谁在买美债
```

**为什么是 list item 而不是 markdown 表格**：Dataview / Bases 的查询单位是**笔记**。断言若是笔记里的一张表格，跨集查询查不出来。行内字段挂在 list item 上，配合 `FLATTEN file.lists` 可做行级查询，且不需要一条断言一个文件（几百集下来会是几万个碎文件）。

> ⚠️ **待验证**：上面那种「续行写字段」的写法是否真的被 Dataview 挂到父 list item 上，**尚未实测**。若不成立，退路是全部字段挤在一行（丑但可靠）。这是 prototype 第一件要验的事（§10）。

**不要打算阅读 EP 笔记的原始 markdown**：一条断言 12 行、一集 30 条就是 360 行。EP 笔记是**容器不是阅读界面**——阅读界面是渲染出来的 Dataview 表和 `15-Materials/` 里的精简材料。

### 5.2 `type` 五类（单值，主轴）

| type | 判据 | 专属字段 |
|---|---|---|
| `fact` | 可核查的事实陈述（过去或当下） | — |
| `causal` | **给出了机制**——能填得出 cause/effect | `cause` `effect` |
| `prediction` | 关于未来的陈述 | `前提条件原文` `时限原文` `时限类型` `时限规范` `应验判据原文` `检验状态` |
| `judgment` | 价值判断，不可核查 | — |
| `channel` | 「某某可在某处查到」/「想懂这个该看谁」 | `渠道类型` `名称` `作者或机构` `取数地址` `链接` `原话口径` `为什么值得看` |

**优先级（type 单值，冲突时按此判）**：`channel` > `prediction` > `causal` > `judgment` > `fact`。理由：前三者带专属字段，必须触发；`judgment` 与 `fact` 的区别在可核查性。边界模糊时以「**能否填得出 cause/effect**」为准。实跑后若发现常打架，再议。

**关于 `causal` 必须带说话人**：政经直播里的因果解释**几乎全部是「某人的解释」，不是客观事实**。`谁说的` 字段不是冗余——它让「关于同一段历史，三个主播给出三种不同因果链」成为可查的结构，而这种分歧本身就是目的 (B) 的核心素材。前身 spec 把关系存成无主的边「小布什 —提出→ 八人帮」，机制和说话人双双丢失，是本次重写要修的主要缺陷。

**`channel` 为什么升为一等公民**：它直接就是目的 (B)。抽取「某某数据可在某某地方看到」这类句子并归档，**简单、立即有用、且真正稀缺**。如果只能先做一片，就做这片。

**`channel` 是行不是列**：前身规划把「信息渠道」设计成断言表的一列。做成列有两个问题——它得挂在**某一条**断言上（但渠道提及前后常常没有对应断言，或同时关联好几条），而且这一列在 95% 的行上是空的。做成独立的一行后，它白嫖全套已有机制（三闸门、`source_quote`、录音时间戳、审核动线全不变），而整个渠道索引就是一句 `WHERE type = "channel"`。

**`channel` ≠ `归因`，两者容易混**：

| | 问的是 | 例子 | 方向 |
|---|---|---|---|
| `归因作者` / `归因媒体`（断言的字段） | 他这话**从哪听来的** | 「据 FT 的 Martin Wolf 说，英国国债……」 | 向后：这条陈述的出处 |
| `channel`（独立的一行） | **你**可以去哪查、该看谁 | 「想搞明白这个就去看 Martin Wolf 的专栏」 | 向前：一把你能用的钥匙 |

所以**记者名不是 channel 专属**：它在断言上是 `归因作者`（谁说的），在渠道行上是 `作者或机构`（该看谁）。

由此有一个白捡的推论——**一条带具名归因的断言天然携带一个 channel 的种子**：主播说「据 Martin Wolf 说 X」时同时暴露了两件事，(1) X 这个说法，(2) **他读 Martin Wolf**。第二件就是目的 (B)。因此「他都看什么」这个视图是**两个来源的并集查询**（§6.2），不需要在 schema 上做任何事，也不要为此复制出重复的 channel 行。第二类的数量远大于第一类。

**`channel` 的两种形态**（用 `渠道类型` 区分，不拆成两个 type——拆了会把目的 B 的主查询劈成两半）：

- **数据源**：可反复取数的序列/库（FRED、财政部 TIC 月报、Wind）→ 服务「我要查这个数字」
- **内容源**：一次性的读物或人（某本书、某篇报道、某个专栏、某个访谈）→ 服务「我要学这个视角」

### 5.3 完整字段表

| 字段 | 适用 | 必填 | 说明 |
|---|---|---|---|
| `id` | 全部 | ✅ | 集内唯一，形如 `A-02` |
| 正文（list item 文本） | 全部 | ✅ | **必须自成句**：消解一切悬空指代，遮住原文还能读懂 |
| `type` | 全部 | ✅ | 见 §5.2 |
| `情态` | 全部 | ✅ | `确定` / `推测` / `传闻`。原文带「应该是/大概/可能/我猜」→ `推测`；「据说/听说」→ `传闻`。**这一轴专治语气固化** |
| `谁说的` | 全部 | ✅ | 主播 / 具名嘉宾 / 被转述者 |
| `归因类型` | 全部 | ✅ | `无外部归因` / `具名信源` / `模糊信源` / `匿名私人信源` / `转述他人` |
| `归因媒体` `归因作者` `归因索引` | 有外部信源时 | | 只记原文层 + 指针（`S-xxxx` → `_index/sources.json`）。**绝不自己补链接/记者名** |
| `录音时间戳` | 全部 | ✅ | `HH:MM:SS`，主播在第几分钟说的。回原文用 |
| `事件时间原文` | 全部 | | 照抄原话（「911之后」「上个月」）。原文没提时间则空 |
| `事件时间` | 全部 | | 规范化 ISO，**区间起点**。排序键 |
| `事件时间止` | 全部 | | 区间终点，可空。渐变过程（「1690s–1780s 文官集团逐步…」）靠它 |
| `事件时间精度` | 有 `事件时间` 时 | ✅ | `day` / `month` / `year` / `decade` / `era` / `未知` |
| `cause` `effect` | `causal` | ✅ | **自由文本，宁可啰嗦**。见 §5.5 |
| `前提条件原文` | `prediction` | | 前提没发生 ≠ 预测被证伪，必须独立成字段 |
| `时限原文` `时限类型` `时限规范` | `prediction` | | `时限类型 ∈ {日期锚定, 事件锚定}`；事件锚定 → `时限规范: null`，**不强转日期** |
| `应验判据原文` | `prediction` | | 照抄「怎样算兑现了」 |
| `检验状态` | `prediction` | ✅ | `待检验` / `已兑现` / `已证伪` / `前提未发生` / `无法判定` |
| `渠道类型` | `channel` | ✅ | `数据源` / `书` / `报道` / `专栏` / `访谈` / `播客` / `报告` / `机构文件` / `人`。枚举实跑后可调 |
| `名称` | `channel` | ✅ | FRED /《大转型》/ Martin Wolf 的 FT 专栏 |
| `作者或机构` | `channel` | | 记者名在渠道行上的落点（区别于断言上的 `归因作者`，见 §5.2） |
| `取数地址` | `channel` 且 `渠道类型: 数据源` | ✅ | **具体到怎么找**（哪张表、哪个页面），不是泛泛的站名 |
| `链接` | `channel` | | **多半为空**——主播不会念 URL，而抽取不得脑补，**不许编**。补全是按需的活，不是抽取的活 |
| `原话口径` | `channel` | ✅ | 他说的是「谁在买」，不是「持有人结构」。口径不留，将来和数据源对不上 |
| `为什么值得看` | `channel` | | 他若说了理由（「他那个模型比较靠谱」「这家一直提前一个月」）就照抄。**目的 (B) 要的不只是「他看什么」，是「他为什么信它」——那才是分析框架**。典型的廉价保险：照抄一句 vs 事后重听 3 小时 |
| `实体` | 全部 | ✅ | `[[双链]]` 列表 |
| `主题` | 全部 | 见右 | 受控词表，**多值**（逗号分隔，Dataview 解析为列表：`主题:: 财政军事国家, 战时授权`）。断言行阶段 2 留空、阶段 5 回填；**`channel` 行是例外，阶段 3 当场填，必填**（§5.4） |
| `素材` | 全部 | | `是` / 空。疑似好故事/金句/轶事/人物细节。**宁多标由人删，漏标很贵** |
| `source_quote` | 全部 | ✅ | 逐字引文，**可多条**（1..N）。每条都必须是**语义自足的完整句**，不许取半截残句；宁可多带一两句上下文。多句证据就多放几条 |
| `存疑原因` | ⛔ 时 | | 机器自觉可疑就填 |

**关于 `事件时间` 与前身 spec 的关系**：前身的 stage2 prompt v0.1 已有 `事发时间{值,粒度,原文}`（#7 原型评审产物），本次把它**从一个抽取字段提升为一等索引键**——它是「按人物/主题拉时间轴」这个查询能成立的唯一前提。字段名统一为上表。

**三种时间不要混**：

| | 是什么 | 用途 |
|---|---|---|
| `录音时间戳` | 主播在第几分钟说的 | 回原文听原话 |
| `事件时间` | 事情发生在哪一年 | **时间轴排序** |
| `播出日期`（集级 frontmatter） | 这集什么时候录的 | 时事断言的「现在」是哪天。少了它「上个月」永远解析不了 |

**定不了年的，绝不许猜。** 「殖民时期」「很久以前」→ `事件时间:: `（空）+ `事件时间精度:: 未知`，原文照留，排序沉底。让 LLM 猜年份直接撞「不得脑补」红线，而且**错误的年份比缺失的年份危害大得多**——它会安安静静地插在时间轴中间。

### 5.4 主题词表

`主题` 是**受控词表**，人工维护，存 `_index/topics.md`，每个词写一句「什么算、什么不算」，**只增不改**（改就是重构索引）。

**为什么这一项必须受控，而 `cause`/`effect` 可以自由**：`cause`/`effect` 是给人读的，漂了只是啰嗦；**`主题` 是索引键，漂了就废了**。第一集写「英国财政」、第五集写「财政军事国家」、第十集写「国家汲取能力」——三个标签指同一件事，查询永远只捞回三分之一。

受控在这里可行（而实体消歧不可行）：数量小（几十个封顶）、由人维护、且——

> **这份词表本身就是产出物，不是元数据。**
> 分析框架是什么？就是一个人反复用来切割世界的那几十个角度。攒下来的主题词表，就是目的 (B) 显影出来的样子。它长什么样、哪几个标签被打得最多、什么时候意识到需要新开一个——这些比任何单条断言都更接近用户想要的东西。

**`主题` 是多值字段**：一条断言可以同时属于几个主题，逗号分隔（`主题:: 财政军事国家, 战时授权`），Dataview 解析为列表，用 `contains(L.主题, "…")` 查。

**落点是阶段 5 读材料时，不是阶段 3 审断言时。** 精简材料本来就是按主题分段的，读的时候主题是现成的，把段落主题挂回该段涉及的断言行，边际成本最低；审断言表时逐条想主题反而是最贵的时刻。

> **例外：`channel` 行必须在阶段 3 当场填，且必填。**
> 因为两者的使用方式相反——**断言表是顺着读的**（主题在读的时候浮现），**渠道索引是反着查的**（「我想查美债数据，去哪儿？」）。**没有主题，渠道索引就只是一堆无序的地址，你根本找不到它**。
> 可行性没问题：渠道行一集大概 3–8 条，比几十条断言便宜得多，而且当场你还记得上下文。

**种子期提醒**：头几集词表是空的，最早的 `channel` 行只能边填边造词。这就是词表的种子期，别指望第一集就整齐——也别为此提前编一份词表（§11）。

**「LLM 提候选 + 人勾选」的机制**（与 #30 实体消歧同构，直接抄）：

- LLM **只能从现有词表里选**，选中的直接填
- 都不合适 → 提议新词 + 理由，**挂起等人点头**，不自动入库
- 点头的落进词表，下次可直接用

词表的增长权始终在人手里，LLM 干的是「查表匹配」这种蠢活。

### 5.5 `cause` / `effect` 是自由文本，且宁可啰嗦

```
❌ effect:: 可信承诺
✅ effect:: 国家对外借钱时，债权人相信这笔钱将来会被还上
```

**代价是明确的**：跨行按 `effect` 字面匹配这条路废了。三个主播讲同一件事会写出三句不同的长句，对不齐。

**这不算损失**，因为跨行查询本来就该走另外两条路：**按实体查** + **按事件时间排序**。`cause`/`effect` 退回它真正擅长的角色——**给人读的、把机制说清楚的字段，不是索引键**。

不上受控词表的理由：命题比人名更难标准化，而「一上来就建正式图谱」正是个人项目最常见的死法。先按自由文本跑；若干集之后如果确实出现「明明记得有人讲过相反的因果，但查不出来」，那时再上受控词表——而且那时手里已经有真实数据知道该控哪些词。

### 5.6 暂不实现：状态快照表

前身规划里的「第 2 层状态快照表」（同一实体在不同时间点的属性快照，用于让渐变过程显形）**暂不单独实现**。理由：带 `事件时间` + `事件时间止` 的断言行已经部分承担了这个功能——把同一实体的行按事件时间排序，演化就会显形。

**触发条件**：实际用过一段时间，**确实感到「看不出演化」**再加。不要提前建。

---

## 6. Obsidian Vault 结构

```
Vault/
├── 10-Episodes/       EP 笔记：断言行的物理容器 + 逐字稿回链 + 集级 frontmatter
├── 15-Materials/      精简材料（按主题分段，人的主阅读面）
├── 30-Threads/        脉络笔记（人写，永不自动生成）
├── _review/           人工核对暂存区（不参与图谱）
├── _pairs/            校对配对 pre/post 快照（永久资产，不参与图谱）
├── _queries/          保存的查询（待检验预测队列、人物时间轴、主题视图…）
└── _index/
    ├── entities.json  实体索引（程序读写，非面向人）
    ├── sources.json   归因补全（置信度与依据必填）
    └── topics.md      主题词表（人工维护，只增不改）
```

**砍掉了 `20-People/` `21-Events/` `22-Places/` `23-Concepts/` `24-Periods/`**——不再自动生成实体笔记。

这带来一个**看似是 bug、实际是 feature** 的结果：断言行里的 `[[小布什]]` 指向一个不存在的笔记，是「未解析链接」。Obsidian 完全支持这个状态，而且：

> **你想给某个实体建笔记时，点一下就有了，且它的反链面板里已经躺着全部相关断言行。**

实体笔记从「自动生成的一堆你不会读的存根」变成「你需要时手动创建、内容自动填充」。这正好符合 §1.3 的职责边界。

### 6.1 EP 笔记 frontmatter

```yaml
---
type: episode
episode: EP01
title: 外国如何干涉美国选举
播出日期: 2026-07-20        # 时事断言的「现在」是哪天
主播: [xxx]
transcript: ../_assets/EP01.transcript.json
derived_from: EP01.transcript.repaired.md
engine: claude-opus-5
prompt_version: stage2-extract@v0.3
generated_at: 2026-07-27
---
```

### 6.2 必备的保存查询（`_queries/`）

**待检验预测队列**（周检用，§4.6）：

```dataview
TABLE L.事件时间原文, L.应验判据原文, L.谁说的, file.link
FROM "10-Episodes"
FLATTEN file.lists AS L
WHERE L.type = "prediction" AND L.检验状态 = "待检验"
SORT L.录音时间戳 ASC
```

**人物时间轴**（把 `小布什` 换成任意实体）：

```dataview
TABLE L.事件时间, L.事件时间精度, L.text, L.谁说的, file.link
FROM "10-Episodes"
FLATTEN file.lists AS L
WHERE contains(L.实体, "小布什")
SORT L.事件时间 ASC
```

**渠道索引**（目的 B 的主产出），他**明说**「你们去看」的：

```dataview
TABLE L.渠道类型, L.名称, L.作者或机构, L.取数地址, L.为什么值得看, file.link
FROM "10-Episodes"
FLATTEN file.lists AS L
WHERE L.type = "channel"
SORT L.渠道类型, L.名称
```

按主题翻（实际使用形态——你是反着查这张表的）：`… WHERE L.type = "channel" AND contains(L.主题, "美债")`

**「他都看什么」＝并集视图**（§5.2）。上面那条只捞到明说的部分；他**没明说但在用**的信源藏在断言的归因里，数量远大于前者：

```dataview
TABLE L.归因媒体, L.归因作者, L.text, file.link
FROM "10-Episodes"
FLATTEN file.lists AS L
WHERE L.归因作者 != null OR L.归因媒体 != null
SORT L.归因媒体
```

两条查询并排放在 `_queries/` 的同一个笔记里即可，**不要为此复制出重复的 channel 行**。

**同一结果的不同解释**（分歧素材）：按 `实体` 筛 + `type = causal`，人读 `cause`/`effect`。**不做字面聚合**（§5.5）。

---

## 7. 模型/工具分工与成本

**全流程 LLM 统一走 Claude Code 订阅额度**（#14 实测后拍板）。理由不是省事，是 Flash 在关系/论断维度不达标、本地模型引文纪律不达标。

| 环节 | 模型/工具 | `--model` | `--effort` |
|---|---|---|---|
| 阶段 0 语音转写 | 本地 faster-whisper large-v3（PC/5070） | — | — |
| 阶段 0 逐字稿修复 pass | Claude Code 无头 | `sonnet` | `low` |
| 阶段 1 分块 | 纯代码 | — | — |
| 阶段 2 抽取 | Claude Code 无头 | `opus` | `high` |
| 阶段 2.5 建议合并 | Claude Code 无头 | `sonnet` | `medium` |
| 阶段 3 审核面渲染 | 纯代码 | — | — |
| 阶段 4a 落库 | 纯代码 | — | — |
| 阶段 4b-1 主题骨架 | Claude Code | `opus` | `medium` ⚠️待实测 |
| 阶段 4b-2 逐主题写段落 | Claude Code | `sonnet` | `medium` ⚠️待实测 |
| 按需 溯源查证（联网） | Claude Code | `opus` | `high` |
| 按需 预测周检（联网） | Claude Code | `sonnet` | `medium` |
| 个人理解与提问（并行，非主链） | NotebookLM | — | — |

- **阶段 0 才是 token 消耗大头，不是阶段 2**：修复 pass 输出长度≈输入长度（4–6 万字进、4–6 万字出），阶段 2 是 1.7 万字进、约 9k token 出。**要压额度先压阶段 0**（换 haiku 或只对低置信段落跑），不要动阶段 2。
- **阶段 4b 是新增的消耗**，输入长（逐字稿）、输出中（材料约 1/10）。第 1 遍输入小但要判断力，第 2 遍输入受控但量大。上表是起手值，**必须实跑一集测**。
- `anthropic` API 直接调用与 Claude Code 订阅是两套独立计费，Pro/Max 订阅额度**不能抵扣 API credits**。
- 同一账号还跑另一个 AFK 项目，共享额度池。建议按「每天 1–2 集」节奏，避免撞每周上限。
- 若 Pro 不够用，优先试 Max 5x，观察两周（`/usage`）后再决定是否上 Max 20x。
- **最大的成本旋钮是阶段 0 和按需查证深度**，不是阶段 2。

---

## 8. 关键设计原则（红线，务必遵守）

1. **转写与整理严格分离**：ASR 只做听写，不允许总结/润色/脑补。原始逐字稿 + 时间戳是唯一事实基础。
2. **提取不得脑补**：只提取逐字稿中明确出现的内容，不许补模型自己的背景知识。即使「知道」原文的日期与公开时间线不符，也**不准断言其错**——照抄进 `事件时间原文`，在 `存疑原因` 标「日期待核」，判断留给按需查证。`source_quote` 是这条红线的机械载体。
3. **精简材料只删只排，不增不综合**（§4b 表）。
4. **限定词与不确定性标记必须原样保留**——阶段 2 靠 `情态` 轴，阶段 4b 靠禁止改口径。这是同一条红线在两处的两个实现，缺一不可。
5. **人工核对是质量闸门，不是可选项**；且**审核只落在提取产物上**，永不要求通读逐字稿。
6. **综合归人**：脉络笔记永不自动生成。这一层就是用户想要的东西本身。
7. **主题词表的增长权在人**：LLM 只能选、只能提议，不能自己往词表里加。
8. **不许猜年份**：定不了年就留空 + 标 `未知`。
9. **同一实体同一名字**：靠 `aliases` 与 `entities.json`；模糊命中永不自动合并。
10. **每个派生文件必须带 provenance**（§9）。
11. **Map-Reduce 而非一次性长文本**：超过约 2 万 token 必须分块独立处理，再对处理结果（而非原文）做归并。阶段 4b 的两遍法是这条原则的一个特例实现。

---

## 9. 资产与 provenance

**策略：全部留下。** 成本可以忽略（纯文本，一集顶多几百 KB），而哪些能删只有跑过才知道。

但「全留」有一个前提，且它符合本项目一贯的「**只在产出当下能拿到，事后补不了**」逻辑：

> **每个派生文件都要记下它是怎么来的。**

```yaml
---
derived_from: EP01.transcript.json
engine: claude-opus-5
prompt_version: stage2-extract@v0.3
generated_at: 2026-07-27
---
```

不留这个，三个月后面对七八种文件，分不清哪些是正本、哪些能重生成、哪些是旧版 prompt 产的——「全留」就退化成「一堆不知道来路的文件」，等于没留。`_pairs` 的 `engine:` 字段本来就已经在这么干，现在推广到所有派生文件。

**将来删的决策也因此变简单**：`derived_from` 齐全 + 输入还在 = 可重生成 = 可删。

**永久正本（永不删）**：逐字稿 JSON、`EP{n}.meta.json`、审核通过后的断言行、`_pairs/`、`_index/*`、人写的脉络笔记。

---

## 10. 开发优先级

1. **打通笔记本↔PC 桥接**（Tailscale+SSH）与远程转写脚本——「一句命令，音频进逐字稿出」
2. **`/prototype` 验证 Dataview 行级查询**：续行行内字段是否挂到父 list item、几千行规模下的手感与性能。**这是唯一一件纸面答不了、且会推翻数据模型的事**（§5.1）
3. **只做 `channel` 抽取**——从逐字稿抽「某数据在某处可查」。最小、立即有用、直接兑现目的 (B)
4. 扩到 `fact` + `prediction`（简单、可核查）+ 三闸门出口校验
5. Obsidian 目录结构 + `_review/` 读写 + 确认信号
6. `entities.json` + 零 API 精确匹配
7. 加 `causal` 与 `judgment`，开始积累不同说话人的因果解释分歧
8. 阶段 4b 精简材料（两遍法）
9. 主题词表 + 「LLM 提候选人勾选」
10. 热词库打磨；参数调优（分块粒度、材料压缩比等）

> **第 ③ 层（手写脉络笔记）不依赖任何软件，现在就能开始。**
> 如果发现「手写脉络」这件事自己根本不做，那说明软件建好了也不会用——**这是一个很好的自检**，比任何技术验证都重要。

---

## 11. 开放问题

- **Dataview 续行行内字段是否成立**（§5.1）——最高优先级，会推翻数据模型
- **对比性论断缺参照系字段**：「同样的税基能撬动数倍军费」隐含对照对象（法国）。当前 schema 会把它压扁成一句没有参照系的话。是加 `对照对象` 字段，还是要求写进正文自成句？未定
- **阶段 4b 的 model/effort**（§7 表）——必须实跑一集测
- **主题词表的初始种子**——跑几集才有，不要提前编
- `_review/` 「确认通过」用什么信号（改文件名 / 加 frontmatter / CLI 参数）
- 分块重叠时长、材料压缩比的具体默认值——先用保守值跑几集再调
- CLI 是否需要批处理队列模式，还是完全手动逐次触发
- Max 5x 是否够用，实跑两周后再定

---

## 12. 本次重写相对前身 spec 的变更清单

| | 前身 | 现在 |
|---|---|---|
| 目标 | 自动生成 Obsidian 知识图谱 | 整理素材、辅助人吸收（§1.1 两条目的） |
| 阶段 0/1 | — | **完全不变**，所有实测结论保留 |
| 抽取产物 | 实体花名册 + 关系 + 论断（三节） | **一张断言表**，`type` 五类单轴 + `情态` 正交轴 |
| 因果 | 无主的关系边 | `causal` 断言，**带说话人**，`cause`/`effect` 自由文本 |
| 事件时间 | 抽取字段之一 | **一等索引键**（三件套 + 区间 + 精度） |
| 存储形态 | 每实体一个笔记 | 断言＝list item；**总库是一条查询**；实体笔记按需手建 |
| 阶段 4 | 自动合并 + 自动查证 + 自动生成实体笔记与 MOC | **砍**。改为落库 + 精简材料生成 |
| 查证 | 流程内环节，每集设条数上限 | **按需工具**，人触发 |
| 消歧 | 含 embedding 层规划 | 瘦身到零 API 精确匹配；**#33 关闭** |
| 精简材料 | 无 | **新增**（§4b），两遍法，人的主阅读面 |
| 主题标签 | 无 | **新增**，受控词表，词表本身是产出物 |
| 四愿景 | 路线图 | 降级/移出（§1.4） |
| 资产 | 分永久/派生 | 全留 + **强制 provenance**（§9） |
